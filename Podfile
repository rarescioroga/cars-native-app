# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :ios, '13.0'
use_frameworks!

def shared_pods
  pod 'Alamofire'
  pod 'ExytePopupView'
end

target 'Cars' do
  inhibit_all_warnings!
  shared_pods
end


target 'Cars (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Cars (macOS)

end
